{
  "/api/v1/payments/create-intent": {
    "post": {
      "summary": "Create a new Stripe Payment Intent",
      "tags": ["Payments"],
      "security": [
        {
          "bearerAuth": []
        }
      ],
      "requestBody": {
        "required": true,
        "content": {
          "application/json": {
            "schema": {
              "type": "object",
              "required": ["amount", "userEmail"],
              "properties": {
                "amount": { 
                  "type": "number", 
                  "example": 50 
                },
                "currency": { 
                  "type": "string", 
                  "example": "usd", 
                  "default": "usd" 
                },
                "userEmail": { 
                  "type": "string", 
                  "example": "customer@example.com" 
                },
                "userId": { 
                  "type": "string", 
                  "example": "60d0fe4f5311236168a109ca" 
                }
              }
            }
          }
        }
      },
      "responses": {
        "201": {
          "description": "Payment Intent created successfully",
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "success": { "type": "boolean", "example": true },
                  "statusCode": { "type": "number", "example": 201 },
                  "message": { "type": "string", "example": "Payment Intent created successfully" },
                  "data": {
                    "type": "object",
                    "properties": {
                      "clientSecret": { "type": "string", "example": "pi_12345_secret_67890" },
                      "transactionId": { "type": "string", "example": "pi_12345" },
                      "paymentRecordId": { "type": "string", "example": "60d0fe4f5311236168a109cb" }
                    }
                  }
                }
              }
            }
          }
        },
        "400": {
          "description": "Validation Error"
        },
        "500": {
          "description": "Server Error or Stripe API Error"
        }
      }
    }
  },
  "/api/v1/payments/create-checkout-session": {
    "post": {
      "summary": "Create a Stripe Checkout Session",
      "tags": ["Payments"],
      "security": [
        {
          "bearerAuth": []
        }
      ],
      "requestBody": {
        "required": true,
        "content": {
          "application/json": {
            "schema": {
              "type": "object",
              "required": ["amount", "productName", "userEmail", "successUrl", "cancelUrl"],
              "properties": {
                "amount": { "type": "number", "example": 50 },
                "currency": { "type": "string", "example": "usd", "default": "usd" },
                "productName": { "type": "string", "example": "Premium Subscription" },
                "userEmail": { "type": "string", "example": "customer@example.com" },
                "userId": { "type": "string", "example": "60d0fe4f5311236168a109ca" },
                "successUrl": { "type": "string", "example": "http://localhost:3000/success" },
                "cancelUrl": { "type": "string", "example": "http://localhost:3000/cancel" }
              }
            }
          }
        }
      },
      "responses": {
        "201": {
          "description": "Checkout Session created successfully",
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "success": { "type": "boolean", "example": true },
                  "statusCode": { "type": "number", "example": 201 },
                  "message": { "type": "string", "example": "Checkout Session created successfully" },
                  "data": {
                    "type": "object",
                    "properties": {
                      "url": { "type": "string", "example": "https://checkout.stripe.com/c/pay/cs_test_..." },
                      "sessionId": { "type": "string", "example": "cs_test_..." },
                      "paymentRecordId": { "type": "string", "example": "60d0fe4f5311236168a109cb" }
                    }
                  }
                }
              }
            }
          }
        },
        "400": {
          "description": "Validation Error"
        }
      }
    }
  },
  "/api/v1/payments/create-connect-account": {
    "post": {
      "summary": "Create or Retrieve Stripe Connect Express Account & Onboarding Link",
      "description": "Automatically extracts user ID and email from Authorization Bearer JWT token.",
      "tags": ["Payments"],
      "security": [
        {
          "bearerAuth": []
        }
      ],
      "requestBody": {
        "required": false,
        "content": {
          "application/json": {
            "schema": {
              "type": "object",
              "properties": {
                "refreshUrl": { "type": "string", "example": "http://localhost:3000/stripe/refresh" },
                "returnUrl": { "type": "string", "example": "http://localhost:3000/stripe/return" }
              }
            }
          }
        }
      },
      "responses": {
        "200": {
          "description": "Stripe Connect onboarding link generated successfully",
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "success": { "type": "boolean", "example": true },
                  "statusCode": { "type": "number", "example": 200 },
                  "message": { "type": "string", "example": "Stripe Connect onboarding link generated successfully" },
                  "data": {
                    "type": "object",
                    "properties": {
                      "url": { "type": "string", "example": "https://connect.stripe.com/setup/s/..." },
                      "stripeAccountId": { "type": "string", "example": "acct_1..." }
                    }
                  }
                }
              }
            }
          }
        },
        "400": { "description": "Validation Error" },
        "404": { "description": "User not found" }
      }
    }
  },
  "/api/v1/payments/connect-account-status/{accountId}": {
    "get": {
      "summary": "Get Stripe Connect Express Account Status & Capabilities",
      "tags": ["Payments"],
      "parameters": [
        {
          "name": "accountId",
          "in": "path",
          "required": true,
          "schema": { "type": "string" },
          "description": "Stripe Connect Account ID (e.g., acct_1...)"
        }
      ],
      "responses": {
        "200": {
          "description": "Stripe Connect account status retrieved successfully"
        },
        "500": {
          "description": "Failed to retrieve account details"
        }
      }
    }
  },
  "/api/v1/payments/user-to-user-checkout-session": {
    "post": {
      "summary": "User → User: Create Stripe Checkout Session directly transferring funds to recipient's connected Stripe account",
      "description": "Creates a Stripe Checkout Session with destination transfer (`payment_intent_data.transfer_data.destination`). Customer information is automatically populated from the Authorization Bearer Token if authenticated.",
      "tags": ["Payments"],
      "security": [
        {
          "bearerAuth": []
        }
      ],
      "requestBody": {
        "required": true,
        "content": {
          "application/json": {
            "schema": {
              "type": "object",
              "required": ["amount"],
              "properties": {
                "amount": { "type": "number", "example": 10, "description": "Payment amount (e.g. 10 USD)" },
                "currency": { "type": "string", "example": "usd", "default": "usd" },
                "title": { "type": "string", "example": "Event Ticket / Service Fee" },
                "recipientId": { "type": "string", "example": "60d0fe4f5311236168a109ca", "description": "MongoDB ID of recipient user" },
                "recipientEmail": { "type": "string", "example": "recipient@example.com" },
                "stripeAccountId": { "type": "string", "example": "acct_1...", "description": "Recipient's Stripe Connect Account ID" },
                "applicationFee": { "type": "number", "example": 1, "description": "Optional platform fee cut" },
                "customerEmail": { "type": "string", "example": "customer@example.com" },
                "successUrl": { "type": "string", "example": "http://localhost:3000/payment/success" },
                "cancelUrl": { "type": "string", "example": "http://localhost:3000/payment/cancel" }
              }
            }
          }
        }
      },
      "responses": {
        "201": {
          "description": "User-to-user Checkout Session created successfully",
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "success": { "type": "boolean", "example": true },
                  "statusCode": { "type": "number", "example": 201 },
                  "message": { "type": "string", "example": "User-to-user Checkout Session created successfully" },
                  "data": {
                    "type": "object",
                    "properties": {
                      "url": { "type": "string", "example": "https://checkout.stripe.com/c/pay/cs_test_..." },
                      "sessionId": { "type": "string", "example": "cs_test_..." },
                      "paymentRecordId": { "type": "string", "example": "60d0fe4f5311236168a109cb" },
                      "destinationStripeAccountId": { "type": "string", "example": "acct_1..." }
                    }
                  }
                }
              }
            }
          }
        },
        "400": { "description": "Validation Error or Recipient user does not have a connected Stripe account" },
        "404": { "description": "Recipient user not found" }
      }
    }
  }
}
